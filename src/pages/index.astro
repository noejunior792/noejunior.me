---
import Layout from "../layouts/Layout.astro";
import SocialLinks from "../components/SocialLinks.astro";
import ContactForm from "../components/ContactForm.astro";
---

<Layout title="Noé Júnior">
  <div class="space-y-8">
    <section class="space-y-4 py-16">
      <h1 class="text-4xl font-bold">Olá, meu nome é Noé Júnior</h1>
      <p class="text-xl text-gray-600 dark:text-gray-400">
        Desenvolvedor de software, empreendedor, e entusiasta de código aberto.
        Nos meus tempos livres gosto de jogar, dormir, ler mangás/manhwas,
        e aprender 'coisas díficeis', é uma vida honesta!.
      </p>
      <SocialLinks />
    </section>
    <section id="secret-form-section" class="text-center py-8">
      <p class="mb-4 text-lg">Alerta: botão vermelho, não clicar!</p>
      <button id="reveal-button" class="px-6 py-3 bg-red-600 text-white font-bold rounded-lg shadow-lg hover:bg-red-700 transition-all duration-300 transform hover:scale-105">
        Não clique aqui
      </button>
    </section>

    <div id="contact-form-wrapper" class="max-h-0 opacity-0 overflow-hidden transition-all duration-1000 ease-in-out">
      <ContactForm />
    </div>

<script>
  const revealButton = document.getElementById('reveal-button');
  const secretSection = document.getElementById('secret-form-section');
  const formWrapper = document.getElementById('contact-form-wrapper');

  revealButton.addEventListener('click', () => {
    secretSection.style.display = 'none';
    formWrapper.classList.remove('max-h-0');
    formWrapper.style.maxHeight = formWrapper.scrollHeight + 'px'; // Set max-height to actual height
    setTimeout(() => {
      formWrapper.classList.remove('opacity-0');
    }, 100);
  });
</script>



</Layout>
